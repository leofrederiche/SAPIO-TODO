import { colors } from "../../global/colors"

export const styles = {
    form: {
        marginTop: 20,
        marginBottom: 40,
    },

    input: {
        borderWidth: 0,
        borderBottomWidth: 1,
        marginLeft: 10,
        marginRight: 10,
        padding: "10px 5px",
        borderColor: colors.primary,
    },

    button: {
        backgroundColor: colors.primary,
        color: colors.active,
        padding: "10px 20px",
        borderRadius: 6,
        border: "none"
    }
}